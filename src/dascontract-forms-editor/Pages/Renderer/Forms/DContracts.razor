

@if (contract != null) {
    @foreach (Form form in contract.Forms) {
        <CascadingValue Value="@form">
            <DForm></DForm>
        </CascadingValue>
        <br />
        <br />
    }
}

@code {
    [CascadingParameter]
    DContract contract { get; set; }
}